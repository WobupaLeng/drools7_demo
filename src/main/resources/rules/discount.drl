package rules;
import model.Car;

rule "car-discount>30"
agenda-group "dose"
when
    $car : Car(person.age > 30)
    then
        $car.setDiscount(80);
    end

rule "car-discount<=30"
agenda-group "dose"
when
    $car : Car(person.age <= 30)
    then
        $car.setDiscount(90);
    end

